#ifndef MULTIPLES_H
#define MULTIPLES_H

#include <stdio.h>

/**
 * isMultiple - Determines if the second integer is a multiple of the first.
 * @a: The first integer.
 * @b: The second integer.
 *
 * Return: 1 if b is a multiple of a, otherwise 0.
 */
int isMultiple(int a, int b);

#endif /* MULTIPLES_H */
