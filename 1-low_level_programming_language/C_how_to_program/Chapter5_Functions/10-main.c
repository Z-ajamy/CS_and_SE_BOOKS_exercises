#include "prime.h"

/**
 * main - Entry point of the program.
 *
 * Description: Calls `printPrimes` to display prime numbers up to 10,000
 * using both methods and compare their performance.
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
    printPrimes(10000);
    return (0);
}
