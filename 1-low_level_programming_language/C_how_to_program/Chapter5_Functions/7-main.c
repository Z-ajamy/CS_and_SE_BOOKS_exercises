#include "temperature.h"

/**
 * main - Entry point of the program.
 *
 * Description: Calls the function to print temperature conversion tables.
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
    printTemperatureTables();
    return (0);
}
